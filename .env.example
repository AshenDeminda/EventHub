# Example environment file for local/prod Docker Compose
# Copy to .env and adjust values. Do NOT commit real secrets.

# Frontend (Create React App reads at build-time)
REACT_APP_API_URL=http://YOUR_EC2_PUBLIC_IP:5000

# Backend
JWT_SECRET=change-me-to-a-long-random-string

# MongoDB
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123
MONGO_INITDB_DATABASE=event-scheduler
